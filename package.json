{
  "name": "helios-launch-hub",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Helios GA launch hub: pipeline dashboard, CRM, stakeholder plan and enablement materials, live from Google Sheets/Docs.",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "test": "node --test",
    "snapshot": "node scripts/snapshot.mjs"
  },
  "dependencies": {
    "@vercel/functions": "^2.0.0"
  },
  "engines": { "node": ">=20" }
}
